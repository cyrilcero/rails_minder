<%= form_with(model: category, class: "contents") do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>
      <ul>
        <% category.errors.each do |error| %>
          <li>❌ <%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="my-5">
    <%= form.label :name, class: "font-thin"  %>
    <%= form.text_field :name, class: "rounded-lg w-full border-2 border-slate-800" %>
  </div>
  <div class="flex flex-col">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-green-300 font-medium cursor-pointer text-center" %>
    <%= link_to "Back", :back, class: "rounded-lg py-3 px-5 font-medium text-center" %>
  </div>
<% end %>
